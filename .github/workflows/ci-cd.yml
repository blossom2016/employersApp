name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest

    services:
      docker:
        image: docker:20.10.7
        options: --privileged

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Backend Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/backend:latest ./nodeapp/backend
          docker push ${{ secrets.DOCKER_USERNAME }}/backend:latest

      # - name: Build and Push Frontend Image
      #   run: |
      #     docker build -t your-dockerhub-username/frontend ./nodeapp/frontend
      #     docker push your-dockerhub-username/frontend

      - name: Deploy to Kubernetes (if applicable)
        run: |
          echo "Kubernetes deployment commands here"
